---
/**
 * Landing en español (idioma por defecto, en la raíz "/").
 */
import BaseLayout from '@layouts/BaseLayout.astro';
import Landing from '@components/Landing.astro';

const lang = 'es' as const;
---

<BaseLayout lang={lang}>
  <Landing lang={lang} />
</BaseLayout>
