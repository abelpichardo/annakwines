---
/**
 * Composición de la landing de una sola página: Nav + todas las secciones
 * dentro de `.shell` + Footer. Recibe `lang` y lo propaga a cada sección.
 */
import Nav from '@components/layout/Nav.astro';
import Footer from '@components/layout/Footer.astro';
import Hero from '@components/sections/Hero.astro';
import About from '@components/sections/About.astro';
import Bridge from '@components/sections/Bridge.astro';
import Services from '@components/sections/Services.astro';
import Credentials from '@components/sections/Credentials.astro';
import Judge from '@components/sections/Judge.astro';
import Markets from '@components/sections/Markets.astro';
import Testimonials from '@components/sections/Testimonials.astro';
import Contact from '@components/sections/Contact.astro';
import type { Lang } from '@i18n/ui';

interface Props {
  lang: Lang;
}
const { lang } = Astro.props;
---

<Nav lang={lang} />
<div class="shell">
  <main>
    <Hero lang={lang} />
    <About lang={lang} />
    <Bridge lang={lang} />
    <Services lang={lang} />
    <Credentials lang={lang} />
    <Judge lang={lang} />
    <Markets lang={lang} />
    <Testimonials lang={lang} />
    <Contact lang={lang} />
  </main>
  <Footer lang={lang} />
</div>
